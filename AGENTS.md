# Agent Instructions — 项目管理案例集

项目管理领域的实践案例与成果展示。**gallery 是案例的公开展示层**——内容可能被公开访问，脱敏是入库的前置条件。

## 脱敏规则（入库前强制执行）

### 一、绝对不入库的内容

| 类别 | 示例 |
|---|---|
| 人名与账号 | 客户姓名/昵称/平台账号、我方成员姓名、对接人变更记录中的名字 |
| 机构信息 | 客户所属院校、企业、平台名称（含可反推的描述，如"海外高校研究者"可保留，"HEC Paris"不可） |
| 项目可识别信息 | 项目代号、仓库名、数据文件名、交付文件名 |
| 数据与规模 | 用户数量、数据量、具体金额、工单号、发票信息 |
| 技术栈与字段 | 具体数据源名、指标名、字段名、表名、外部服务名（Dropbox/BigQuery 等） |
| 精确时间 | 具体日期（2026-07-16）——只保留粒度到"阶段"的时间叙事 |

### 二、允许保留的信息（辨识度下限）

案例需要"帮助读者判断是哪类问题"的模糊信息：

- **业务形态**：面板数据项目、分阶段交付、外部学术客户、数据工程项目
- **问题类型**：统计口径歧义、数据源下线、验收口径拼合、工具链缺陷
- **教训本体**：口径必须用算例对齐、数据资产要沉淀、验收标准要前置——这些是案例的存在理由
- **结构信息**：角色构成（商务经理/软件工程师各 1）、周期量级（约半年）、状态（已交付待验收）

判断口径：**读完整篇能知道"这是一类什么问题、教训是什么"，但无法知道"是哪个客户、哪个项目、哪条数据"。**

### 三、写法要求

1. **先脱敏再写作**：不是写完再删——第一稿就用脱敏后的语言组织，避免"敏感原文 → 机械替换"留下痕迹
2. **替换要语义化**：GHTorrent → "某公开技术社区的历史数据集"（保留"外部大体量数据源"的语义），不是 GHTorrent → "某数据源"（丢失教学价值）
3. **教训保留完整细节**：口径混淆的两类区分、字段不可靠的技术结论——这些是案例价值，不属于敏感信息
4. **待填事实不保留空节**：原报告缺失的数据（响应时效等）直接不写，不留"（待填写）"
5. **结构重写**：gallery 是教学叙事（概况 → 过程 → 发现 → 改进 → 启示），不是复盘报告的归档格式（归档版在项目文档目录，遵循复盘章程第二十条）

## Git 历史纪律

**脱敏必须从第一个提交就成立。** 修正敏感内容时禁止普通 commit（旧版本仍在历史中可达）：

- 首次入库即脱敏后的版本；
- 若已发生敏感内容入库：重写历史（`git reset --soft <init>` 重建提交），**force push 覆盖远端**；
- 注意：GitHub 上被 force push 丢弃的提交可能仍可通过 SHA 直达（缓存/悬空对象），敏感度高的内容需联系 GitHub Support 清理——所以**唯一可靠的策略是第一次就写对**。

## 其他纪律

1. 每次 commit 后自动 push；分层提交：本库 → quanttide-project → 根仓库
2. 子仓库操作前 `git checkout main && git pull`（常分离头，分离头提交用 ff-only 修复）
3. stage/ 是案例的工作空间，内容在此持续维护
4. 复盘报告 → 案例 的改写权：报告归档版保留全部事实（含人名，按章程第十七条以角色替代），gallery 版按本规则脱敏
